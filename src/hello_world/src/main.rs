// $ rustc main.rs -o main && ./main

// $ rm -rf dist && mkdir dist
// $ rustc src/main.rs -o ./dist/main && ./dist/main

// $ cargo run
// $ cargo run hello_world
// cargo run 入口文件 `src/lib.rs` 或 `src/lib.rs`

fn main() {
  println!("🦀 Hello, world!");
}
